 1. What is Semantic HTML?- It is the proper use of HTML in order reinforce or emphasize and a create meaning to the webpage.

 2. What is HTML used for?- HTML is used to create the structure and content of a webpage.

 3. What is an attribute and where do we put it?- Attributes are used to give more information about the element. The attribute is placed inside the opening tag of the element.

 4. What is the h1 tag used for? How many times should I use it on a page? The h1 tag is used for the heading of the webpage. It should only be used once on a page.

 5. Name two tags that have required attributes: <img>, <a>

 6. What do we put in the head of our HTML document? <header> tags

 7. What is an id? ID is an attribute used to identify an elemnent uniquely.

 8. What elements can I add an id to? Any HTML element

 9. How many times can I use the same id on a page? Once
 10. What is a class? Class is an attribute similar to 'id' except you can use it multiple times with other elements.

 11. What elements can I add a class to? Any

 12. How many times can I use the same class on a page? Infinite!

 13. How do I get my link to open in a new tab? Set the target attribute to _blank, after the href url.

 14. What is the alt attribute in the image tag used for? To explain the image to anyone who may have a disablitly and cannot see your image.

 15. How do I reference an id? in CSS put #insert-id-here, then brackets and your CSS code within those brackets.

 16. What is the difference between a section and a div? The section tag can be used to define a chapter, footer, or even a header on the page. A div shows a division in the page and cvan be used to group block elemnents.

 17. What is CSS used for? CSS is used to style your webpage. CSS is used to create the aesthtic of a page.

 18. How to we select an element? Example - every h2 on the page. For CSS just type the element before the two brackets : h2 {css:code;}.

 19. What is the difference between a class and an id? - Give me an example of when I might use each one. You can olny use an id once, it is unique to the element. Class can be used in multiple elements. Examples are if you wanted to style a bunch of pictures then you would put a class in the line of code of all the pictures. If you wanted to only style the header than you can you an id.

 20. How do we select classes in CSS? .class {}

 21. How do we select a p element with a single class of “human””? p.human

 22. What is a parent child selector? When would this be useful? It selects all elements that are a chilf of the specified element. It  can be usefl when you need to find direct children of the element.

 23. How do you select all links within a div with the class of sidebar?.sidebar a:link {}

 24. What is a pseudo selector? It allows you to style a specific part of the selected element.

 25. What do we use the change the spacing between lines? line-height property.

 26. What do we use to change the spacing between letters? letter-spacing property.

 27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform: uppercase; text-transform: lowercase; text-transform: capitalize;

 28. How do I add a 1px border around my div that is dotted and black? div {border: 1px dotted black}

 29. How do I select everything on the page? *
 30. How do I write a comment in CSS? /* insert comment */

 31. How do I find out what file I am in, when I am using the command line? pwd

 32. Using the command line - how do I see a list of files/folders in my current folder? ls

 33. How do I remove a file via the command line? Why do I have to be careful with this? rm -r insert-file-here, it will delete everything and could potential break your system if used incorrectly.  
 34. Why should I use version control? You can get the same task done faster instead of using a mouse to click and  drag folders around.
 
 35. How often should I commit to github? As often as possible.

 36. What is the command we would use to push our repo up to github? git push 'project-name' 'branch-name'

 37. Walk me through Lambda's git flow. first you fork the repo that you want. Then you clone the repo using Git Bash(in my case) then create a branh using your name. Once the coding is done make a pull request on Github and that would be the equivalent of turning in an assignment at Lambda.

Stretch Questions

    1. What is the difference between an inline element and a block element? Block elements usually cause a line break but inline elements do not.

    2. What happens when an element is positioned absolutely? The element wil be on its own layers seperate.

    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? I think you can just set margin to 0 and still be able to adjust the width.

    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default: inline block is button. inline defalut are span, and img. elements that are diplay block by default article, footer, and nav.

    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? the box model is a box that surrounds every element. if the box is a block box then all elements will respect the properties.